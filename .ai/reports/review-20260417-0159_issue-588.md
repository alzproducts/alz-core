# Code Review: Issue #588 — Migrate Category/Brand/Order/Customer to View + Assembler pattern

**Date:** 2026-04-17
**Branch:** feature/588-migrate-category-brand-order-customer-view-assembler
**Base:** origin/develop
**Files reviewed:** 35 changed + 16 untracked (35 mods, 2 deletions, 14 new code files, 2 migrations, 3 docs)

## Findings

### CRITICAL
_None._

### HIGH
_None._

### MEDIUM
- [app/Infrastructure/Catalog/Order/Mappers/OrderViewAssembler.php:37-56] `buildStatus()` re-implements the `tryFrom` + `Log::error` + fallback pattern already provided by `MapperHelperTrait::buildEnum()`. The plan (`.ai/plans/2026-04-17_588-...md`) explicitly calls for reuse of that trait; OrderModelMapper demonstrates the canonical usage. — **Deferred**

### LOW
- [database/migrations/2026_04_17_000001_create_shopwired_orders_view.php:37 + app/Infrastructure/Catalog/Order/Models/OrderViewModel.php:30] `customer_id` is projected through the SQL view and declared on the Eloquent model but never read by `OrderViewAssembler` or included in `OrderView`. YAGNI — should be added when a consumer needs it. — **Deferred**
- [app/Infrastructure/Catalog/Order/Mappers/*, app/Infrastructure/Customer/Mappers/*, app/Domain/Catalog/Order/ValueObjects/*, app/Domain/Customer/ValueObjects/CustomerView.php] New read-side VOs and assemblers ship without unit tests. Reasonable since no endpoint consumes them yet; tests will land with the consumer endpoint work. — **Deferred**

## Positive Observations
- `Money::inclusiveFromString()` is a tight factory: reuses the private constructor's non-negative assertion, adds only a `numeric` guard, and preserves `decimal(14,6)` precision by skipping `round()`. Unit tests cover precision, integer strings, currency override, non-numeric, and negative rejection.
- Typed-enum migration is complete end-to-end — `list<BrandInclude>` / `list<CategoryInclude>` flow through Contracts → UseCases → Repositories → Resources, with `array_map(BrandInclude::fromValue(...))` at the Presentation boundary providing defense-in-depth alongside DTO validation rules.
- `ShopwiredServiceProvider` correctly relocated the contextual `CustomFieldFactory` binding from `EloquentBrandRepository`/`EloquentCategoryRepository` to `BrandViewAssembler`/`CategoryViewAssembler`, and the four new assemblers are registered + advertised via `provides()` for deferred-provider correctness.
- The two new SQL views (`shopwired.orders_view`, `shopwired.customers_view`) are slim projections — `orders_view` is built on `shopwired.orders_deduplicated` so edited-order duplicates are filtered automatically, matching the "View + Assembler" read-side philosophy.
- Complexity baseline: two entries for the deleted inline `toViewDomain()` methods were **removed** (not re-added elsewhere) — the extraction genuinely reduced complexity rather than shuffling it.

## Summary
The Category/Brand pipeline migration is clean and conservative; Order/Customer arrives as a scaffolded foundation (VO + Assembler + ViewModel + SQL view) with no consumer endpoints wired yet, matching the declared scope. The three findings are all minor consistency/YAGNI items, all deferred — none block the merge. Main follow-up risk: if the OrderViewAssembler enum pattern stays inline, future mappers may copy the drifted pattern rather than the shared trait.

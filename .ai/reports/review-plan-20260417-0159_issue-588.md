# Implementation Plan: Issue #588 follow-ups

**Review report:** `.ai/reports/review-20260417-0159_issue-588.md`

All findings were deferred during triage — capture here so they aren't lost.

## Findings to Address

### 1. OrderViewAssembler should reuse MapperHelperTrait::buildEnum [MEDIUM]
- **Location:** `app/Infrastructure/Catalog/Order/Mappers/OrderViewAssembler.php:37-56`
- **Fix:** Add `use App\Infrastructure\Concerns\MapperHelperTrait;` to the class, then replace the inline `tryFrom` + `Log::error` + fallback block with:
  ```php
  $statusType = self::buildEnum(
      OrderStatusType::class,
      $model->status_name,
      OrderStatusType::Processing,
      $model->external_id,
      'status_name',
  );
  ```
  Drop the now-unused `Illuminate\Support\Facades\Log` import.
- **Why:** Consolidates error-handling pattern, matches the plan's stated approach and `OrderModelMapper::buildStatus()`, and prevents future drift when the trait evolves (e.g., Sentry tags, severity tuning).

### 2. Drop unused `customer_id` projection from orders_view [LOW]
- **Location:** `database/migrations/2026_04_17_000001_create_shopwired_orders_view.php:37` + `app/Infrastructure/Catalog/Order/Models/OrderViewModel.php:30`
- **Fix:** Either (a) create a new migration that `CREATE OR REPLACE VIEW`s without `customer_id` and remove the `@property` from `OrderViewModel`, **or** (b) leave until the consumer endpoint arrives and add it with confirmed usage.
- **Why:** YAGNI — unused projections are invisible maintenance burden and hide downstream bugs (e.g., a consumer reads the column before the schema is validated).

### 3. Unit tests for Order/Customer view layer [LOW]
- **Location:** `tests/Unit/Domain/Catalog/Order/ValueObjects/`, `tests/Unit/Domain/Customer/ValueObjects/`, `tests/Unit/Infrastructure/Catalog/Order/Mappers/`, `tests/Unit/Infrastructure/Customer/Mappers/`
- **Fix:** Pair the test work with the consumer endpoint issue. Minimum coverage when that lands:
  - `OrderViewAssembler`: happy-path mapping, unknown `status_name` → fallback + Log::error assertion
  - `CustomerViewAssembler`: happy-path mapping
  - `OrderView` / `OrderCustomerSummary` / `CustomerView`: constructor shape and `CustomerView::fullName()` trimming behaviour
- **Why:** Classes are currently only exercised implicitly through the service container registration. Adding tests now would mock throwaway fixtures; pairing with endpoint work gives real inputs.

## Suggested Order
1. (MEDIUM) #1 — OrderViewAssembler trait reuse. Smallest change, highest drift risk. Can ship standalone.
2. (LOW) #2 — Decide customer_id fate when the first Order consumer endpoint is scoped; tie the column's existence to a concrete reader.
3. (LOW) #3 — Add tests alongside the Order/Customer consumer endpoint issue.

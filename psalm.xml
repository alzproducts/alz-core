<?xml version="1.0"?>
<psalm
    errorLevel="3"
    resolveFromConfigFile="true"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns="https://getpsalm.org/schema/config"
    xsi:schemaLocation="https://getpsalm.org/schema/config vendor/vimeo/psalm/config.xsd"
    findUnusedBaselineEntry="true"
    findUnusedCode="false"
    cacheDirectory="storage/framework/psalm"
>
    <projectFiles>
        <directory name="app/"/>
        <ignoreFiles>
            <!-- Only list directories that always exist -->
            <!-- Optional dirs (node_modules, examples, .rector-cache, build, coverage-report) -->
            <!-- are omitted - Psalm validates paths exist, breaking CI if they don't -->
            <!-- Since we only analyze app/, these ignores are just belt-and-suspenders anyway -->
            <directory name="vendor/"/>
            <directory name="bootstrap/cache/"/>
            <directory name="storage/"/>
            <directory name="tests/"/>
            <directory name="database/migrations/"/>
        </ignoreFiles>
    </projectFiles>

    <plugins>
        <pluginClass class="Psalm\LaravelPlugin\Plugin"/>
    </plugins>
</psalm>
